import React from 'react';
import './Footer.css';

const Footer = props => (
  <footer className="Footer">
    <h2>Footer</h2>
    <p>This is microapp called "Footer"</p>
    <p>It is rendered with React and styled good old CSS</p>
  </footer>
);

export default Footer;
