import React from 'react';
import './Header.css';

const Header = props => (
  <header className="Header">
    <h2>Header</h2>
    <p>This is microapp called "Header"</p>
    <p>It is rendered with React and styled good old CSS</p>
  </header>
);

export default Header;
